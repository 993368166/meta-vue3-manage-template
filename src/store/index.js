import { useUserStore } from '@/store/user.store';
import { useLoadingStore } from '@/store/loading.store';
import { useRoutesStore } from '@/store/routes.store';
import { useStaticDataStore } from '@/store/staticData.store.js'

export {
    useUserStore,
    useLoadingStore,
    useRoutesStore,
    useStaticDataStore
}
